<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # SPA: serve index.html for client routes (e.g. /accedi refresh)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.html [L]
</IfModule>
